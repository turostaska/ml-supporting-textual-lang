// comments
val intVal = 8

 class UnpairedImageDataset(
    val src: Image2d,
    var trg: Image2d,
    val transform: Compose? = null,
    val unaligned: Boolean = true,
    srcMask: Image2d,
) : Dataset() {
    val foo: Int = 0
    var bar = "asd"

    class Inner {
        val booleanVal = true
        var stringVar = "hello"
        var nullStrVal: String? = null
        val nullVal = null // todo: compilation error, type can't be inferred
    }

}

class Animal(
    val name: String,
) {
    // constructor(
    //     num: Int,
    // ) : this("Animal")
}

var globalBooleanVar = false

